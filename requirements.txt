# Top-level runtime dependencies for QMvisualisation under WSL.
# Native system packages for Manim (cairo, pango, pkg-config, ffmpeg, etc.)
# are installed separately and are intentionally not listed here.

manim==0.19.0
numpy==2.1.3
scipy==1.14.1
python-telegram-bot==21.6
Flask==3.1.2